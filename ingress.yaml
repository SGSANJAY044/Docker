apiVersion : networking.k8s.io/v1
kind: Ingress
metadata:
  name: electric-vechile
spec:
  rules:
  - host: www.electricvechile.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: server-service
            port:
              number: 30416
      - path: /
        pathType: Prefix
        backend:
          service:
            name: client-service
            port:
              number: 32130 
